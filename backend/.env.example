PORT=5000
# Required on Railway — free MongoDB Atlas: https://www.mongodb.com/cloud/atlas/register
MONGODB_URI=
MONGOMS_VERSION=6.0.14
JWT_SECRET=your-super-secret-jwt-key-change-in-production
CLIENT_URL=http://localhost:5173
# PropertyRadar API key — https://developers.propertyradar.com (never expose to frontend)
PROPERTYRADAR_API_KEY=
# Dedicated admin login (created on server start if missing)
ADMIN_PASSWORD=RealistAdmin#2026!
