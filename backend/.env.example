PORT=5000
# Optional — local MongoDB or free MongoDB Atlas cluster
# MONGODB_URI=mongodb://127.0.0.1:27017/realist
# RAM-only in-memory MongoDB 6 when unset (needs ~200 MB free disk for binary)
MONGOMS_VERSION=6.0.14
JWT_SECRET=your-super-secret-jwt-key-change-in-production
CLIENT_URL=http://localhost:5173
# Optional — free 30-day trial: https://api.developer.attomdata.com
ATTOM_API_KEY=

